# Member Role Report — Day 10: Data Pipeline & Data Observability

## 1. Thông tin cá nhân

| Thông tin         | Nội dung                  |
| ------------------ | -------------------------- |
| Họ và tên       | Hà Huy Nhất             |
| MSSV               | 2A202602401                     |
| Khóa/Lớp         | K4              |
| Tên nhóm         | MegaLive     |
| Vai trò chính    | Member (Checkpoint 5)                 |
| Repository         | https://github.com/diggoryQH/K4-L3-DAY10-MegaLive-DataPipeline |
| Ngày hoàn thành | 2026-09-25               |

## 2. Vai trò và phạm vi công việc

### Phần việc sở hữu

| Module/deliverable | File/hàm phụ trách | Input nhận vào | Output bàn giao  | Trạng thái                                 |
| ------------------ | --------------------- | ---------------- | ----------------- | -------------------------------------------- |
| Idempotent Repair & Comparison Report      | `src/pipelines/corruption_flow.py`           | Raw JSON, Clean JSON          | Repaired Index, Repaired Metrics, `corruption_report.md` | Hoàn thành |

### Việc hỗ trợ ngoài phạm vi chính

| Hoạt động                         | Thành viên/module được hỗ trợ | Kết quả                    |
| ------------------------------------ | ------------------------------------ | ---------------------------- |
| Debug Evaluate Pipeline | Nguyễn Văn Việt | Sửa lỗi sai thứ tự truyền tham số trong hàm evaluate_pipeline của hàm Repair |

## 3. Kết quả theo vai trò

| Nhiệm vụ đã thực hiện | File/hàm/artifact liên quan | Kết quả bàn giao       | Cách xác minh         |
| --------------------------- | ----------------------------- | ------------------------- | ----------------------- |
| Chạy luồng Repair | `script/run_corruption_flow.py` | Pipeline chạy thành công | Terminal không có lỗi |
| Báo cáo Corruption Phase | `data/reports/corruption_report.md` | Báo cáo Markdown đối chiếu 3 cột | Mở file xem Hit Rate |

Nêu một output cụ thể mà phần việc của bạn tạo ra hoặc giúp xác minh:
Tạo ra báo cáo `corruption_report.md` minh chứng được khả năng "chữa lành" của hệ thống (Self-healing). Khi dữ liệu bị lỗi, Hit Rate giảm mạnh; nhưng khi khôi phục từ file Raw, Hit Rate lấy lại mốc 100%.

## 4. Giải thích phần kỹ thuật đã thực hiện

### Vấn đề cần giải quyết
Chứng minh được hệ thống có khả năng tự khôi phục dữ liệu nguyên bản và lấy lại phong độ, tạo ra một bảng so sánh trực quan về các metrics qua 3 trạng thái.

### Cách triển khai
Viết pipeline cho `corruption_flow.py`:
- Nạp baseline metrics để làm tham chiếu.
- Gọi hàm corrupt và evaluate nó.
- Chạy Repair bằng cách đọc lại file `data/raw/crossref_records.json` (Source of Truth).
- Làm sạch lại, build index lại, và evaluate lại trên cùng test set cũ.
- Lấy kết quả của cả 3 để build `generate_corruption_report()`.

### Input, output và contract

| Thành phần                   | Mô tả                                     |
| ------------------------------ | ------------------------------------------- |
| Input                          | Baseline Data, Corrupted Data           |
| Output                         | Repaired Metrics và `corruption_report.md` |
| Module phụ thuộc             | `src.evaluation`, `src.ingestion`, `src.retrieval`, `src.observability`                    |
| Module sử dụng output        | Người dùng đọc báo cáo                   |
| Điều kiện lỗi cần xử lý | Pipeline fail khi index không đúng                   |

### Cách xác minh

```bash
python script/run_corruption_flow.py
```

- **Kết quả mong đợi:** Quá trình đánh giá chạy thành công và báo cáo được lưu.
- **Kết quả thực tế:** Code 0, xuất ra báo cáo.
- **Artifact/log:** `data/reports/corruption_report.md`

## 5. Một quyết định kỹ thuật quan trọng

- **Bối cảnh:** Vấn đề truyền tham số cho hàm Repair Dataframe.
- **Các phương án đã cân nhắc:** Sửa trực tiếp trên file corrupted, hoặc đọc lại hoàn toàn từ đầu (Raw).
- **Phương án đã chọn:** Idempotent Repair bằng cách lấy dữ liệu sạch tuyệt đối từ file Raw gốc (`crossref_records.json`).
- **Lý do:** Khi data đã bị hỏng, rất khó để sửa lại chính xác 100%. Việc re-run từ raw layer là phương pháp chuẩn xác nhất trong Data Engineering (Idempotent).
- **Bằng chứng quyết định phù hợp:** Hit Rate trở về chuẩn 1.0.

## 6. Một lỗi hoặc blocker đã xử lý

- **Triệu chứng/lỗi nguyên văn:** Lỗi truyền sai tham số cho hàm evaluate_pipeline tương tự bạn Việt bị mắc.
- **Nguyên nhân gốc:** Copy code cũ sang nên chưa update argument list.
- **Cách xử lý:** Đổi vị trí tham số truyền vào hàm `evaluate_pipeline`.
- **Cách xác minh sau khi sửa:** Chạy lại `python script/run_corruption_flow.py` thành công.
- **Điều học được:** Cần sử dụng Type Hint đầy đủ và kiểm tra kỹ signature của hàm.

## 7. Hiểu biết về luồng end-to-end

1. Dữ liệu đi từ Crossref đến vector index như thế nào?
2. Evaluation set và ground-truth document IDs dùng để đo retrieval/answer quality ra sao?
3. Quality checks khác freshness monitoring ở điểm nào trong bài lab?
4. Vì sao phải dùng cùng test set cho baseline, corrupted và repaired?
5. Repair được xem là thành công dựa trên artifact và metric nào?

**Câu trả lời:**

1. Từ JSON qua API -> lưu file raw -> Pandas Dataframe (được clean, xoá trùng, nối text) -> lưu thành CSV -> dùng Chroma client và SentenceTransformer để nhúng thành vector -> lưu `data_level0.bin`.
2. Các test set được sinh dựa trên dữ liệu ban đầu. Ground-truth ID giúp đối chiếu xem khi search, Chroma có trả ra đúng doc đó không (Hit Rate). Còn Answer Quality thì dùng một LLM Prompt làm giám khảo (Judge) chấm điểm câu trả lời của mô hình với Reference.
3. Quality Checks (GX) kiểm tra về cấu trúc (schema, null, length), còn Freshness SLA kiểm tra về tính thời sự (ngày publish so với hiện tại).
4. Để đảm bảo tính nhất quán (control variable). Nếu đổi test set thì sẽ không biết hiệu năng giảm là do test khó hơn hay do dữ liệu bị bẩn.
5. Khi Hit Rate và F1 Token score quay trở về mức 1.0 (như lúc baseline) trong file `corruption_report.md`.

## 8. Phân tích kết quả

### Metrics chính

| Metric/signal          | Baseline | Corrupted | Repaired | Nhận xét của cá nhân |
| ---------------------- | -------: | --------: | -------: | ------------------------- |
| `retrieval_hit_rate` |      1.0 |       0.6 |      1.0 | Sụt giảm mạnh khi corrupt do nhiễu và xoá summary |
| `mean_token_f1`      |      1.0 |       0.77 |      1.0 | Sụt giảm tương ứng vì không có context tốt |
| `judge_accuracy`     |      1.0 |       0.5 |      1.0 | Tỉ lệ trả lời sai 1 nửa |
| `mean_judge_score`   |      5.0 |       2.8 |      5.0 | Điểm thấp khi bị corrupted |
| Quality checks         |      Passed |       Failed |      Passed | GX phát hiện được dữ liệu sai |
| Freshness status       |      Passed |       Passed |      Passed | Stale records không bị bẫy ở corrupt |

### Kết luận từ số liệu

Hoàn thành hai chuỗi nguyên nhân–bằng chứng sau:

1. Data corruption → quality signal Failed → agent metric (F1/Hit Rate) giảm sút 40%.
2. Repair action → quality signal phục hồi (Passed) → agent metric phục hồi nguyên vẹn 100%.

Corruption nào ảnh hưởng rõ nhất và vì sao?

Tác động rõ rệt nhất là từ Drop Latest Records vì khi data bị mất luôn ở mức index, model mất hẳn 20% knowledge để phản hồi. 

Kết quả nào khác với kỳ vọng ban đầu?

Tôi kỳ vọng Stale Date sẽ làm hệ thống RAG không trả lời được các bài báo mới nhất, nhưng thực ra mô hình ngôn ngữ không tự soi ngày tháng trừ khi câu hỏi yêu cầu rõ ràng, do đó lỗi Stale Date đa số chỉ fail ở Freshness SLA chứ ít tác động đến F1 Token.

## 9. Điều học được và hướng cải thiện

### Ba điều quan trọng nhất

1. Cơ chế Data Idempotent là thứ rất quan trọng trong thiết kế Pipeline.
2. Việc sử dụng Great Expectations để bắt lỗi schema trước khi nạp vào VectorDB là bắt buộc.
3. Cách vận hành End-to-End Pipeline tự động hoá.

### Nếu có thêm thời gian

Tôi sẽ viết unit test chi tiết cho hệ thống khôi phục Repair để đảm bảo 100% dữ liệu không bao giờ bị hỏng.

## 10. Cam kết của thành viên

- [x] Nội dung báo cáo phản ánh đúng phần việc và mức hiểu của tôi.
- [x] Tôi có thể giải thích luồng end-to-end, không chỉ module mình phụ trách.
- [x] Mọi kết luận về kết quả đều có artifact hoặc metric để đối chiếu.
- [x] Tôi không ghi “đã chạy thành công” cho phần chưa được kiểm chứng.
- [x] Báo cáo không chứa `.env`, API key, token hoặc secret.
- [x] Báo cáo này không phải bản sao nguyên văn của báo cáo nhóm hoặc báo cáo thành viên khác.

**Họ và tên:** Hà Huy Nhất
**Ngày xác nhận:** 2026-09-25
